#include <iostream>

using namespace std;

int main()
{
   int a;
        int b; 
        cout <<"nhap so a : ";
        cin  >> a;
        cout <<"nhap so b : ";
        cin  >> b;
       
        cout << " a + b = " << a + b << endl;
	    cout << " a - b = " << a - b << endl;
	    cout << " a * b = " << a * b << endl;
	    cout << " a / b = " << a / b << endl;
	    return 0;
}